%==============================================================================
%  Custom macros — put project-wide notation here so it stays consistent
%==============================================================================

% --- Common ML / probability notation ----------------------------------------
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}              % expectation
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\Normal}{\mathcal{N}}        % Gaussian
\newcommand{\Loss}{\mathcal{L}}
\newcommand{\data}{\mathrm{data}}

\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\diag}{diag}
\DeclareMathOperator{\diver}{div}

% Paired delimiters (auto-sizing): \norm{x}, \norm*{x}, \abs{x}, \inner{x}{y}
\DeclarePairedDelimiter{\abs}{\lvert}{\rvert}
\DeclarePairedDelimiter{\norm}{\lVert}{\rVert}
\DeclarePairedDelimiterX{\inner}[2]{\langle}{\rangle}{#1,#2}

% --- Editorial notes (toggle off with \draftfalse for the final build) --------
\newif\ifdraft \drafttrue
\newcommand{\todo}[1]{\ifdraft{\color{red}\textbf{[TODO: #1]}}\fi}
\newcommand{\note}[1]{\ifdraft{\color{blue}\textit{(#1)}}\fi}
